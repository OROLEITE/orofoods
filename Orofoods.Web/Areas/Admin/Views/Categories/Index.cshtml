@model IReadOnlyList<Orofoods.Web.Models.Catalog.ProductCategory>
@{ ViewData["Title"] = "Categorias"; }
<section class="section"><div class="container"><div class="portal-top"><h1>Categorias</h1><a class="btn btn-gold" asp-action="Edit">Nova categoria</a></div><div class="table-responsive mt-4"><table class="table table-dark table-striped"><thead><tr><th>Ordem</th><th>Nome</th><th>Slug</th><th>Produtos</th><th>Status</th><th></th></tr></thead><tbody>@foreach (var item in Model) { <tr><td>@item.SortOrder</td><td>@item.Name</td><td>@item.Slug</td><td>@item.Products.Count</td><td>@(item.IsActive ? "Ativa" : "Inativa")</td><td class="text-end"><a class="btn btn-sm btn-gold" asp-action="Edit" asp-route-id="@item.Id">Editar</a>@if(item.IsActive){<form asp-action="Deactivate" method="post" class="d-inline"><input type="hidden" name="id" value="@item.Id"/><button class="btn btn-sm btn-outline-light">Desativar</button></form>}</td></tr> }</tbody></table></div></div></section>
